<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Lattice Checkstyle rules. Formatting is owned by Spotless (Palantir Java Format);
  this ruleset covers only what formatting does not: the em-dash ban, Javadoc on
  public API, and naming conventions. Keeping the two concerns disjoint prevents a
  format-vs-lint deadlock.
-->
<module name="Checker">
  <property name="severity" value="error"/>
  <property name="charset" value="UTF-8"/>
  <property name="fileExtensions" value="java"/>

  <!-- Hard project writing rule: no em dash (U+2014) anywhere in Java source. -->
  <module name="RegexpMultiline">
    <property name="format" value="—"/>
    <property name="message"
              value="No em dash (U+2014); use a spaced hyphen, comma, colon, or parentheses."/>
  </module>

  <module name="TreeWalker">
    <!-- Public API must be documented. -->
    <module name="MissingJavadocType">
      <property name="scope" value="public"/>
    </module>
    <module name="MissingJavadocMethod">
      <property name="scope" value="public"/>
      <property name="allowMissingPropertyJavadoc" value="true"/>
    </module>

    <!-- Naming conventions (locked in core_protocol.md). -->
    <module name="TypeName"/>
    <module name="MethodName"/>
    <module name="MemberName"/>
    <module name="ParameterName"/>
    <module name="LocalVariableName"/>
    <module name="ConstantName"/>
    <module name="PackageName"/>
  </module>
</module>
