apiVersion: v2
name: lattice-lib
description: Shared naming, labelling and common environment for every Lattice subchart.
type: library
version: 0.1.0

# Mandatory rather than optional (locked #77): copied into each subchart, the shared helpers would
# become eight copies and recreate the drift the umbrella split removes. Helm makes definitions
# global across a release, so this chart renders nothing and every other subchart calls into it.
