# Feature design

Status: **planned** - feature docs are written as cross-cutting features are designed. Until then this folder holds only this index.

This folder holds **cross-cutting feature design** - behavior that spans more than one service or the mesh (for example cluster interoperability rules, discovery + peering, status aggregation across nodes). One canonical doc per feature; this `_index.md` will become the index.
