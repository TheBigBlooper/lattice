# Status-console UI design

Design docs land here - next session.

This folder holds the **status-console design**: the panel/screen set for viewing node status across a cluster, component standards, and the **design tokens** (theme) the Vite + React console consumes (no hardcoded colors). This `_index.md` will become the UI design overview.
