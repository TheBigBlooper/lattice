# Architecture design

Status: **planned** - these docs are written in the mesh + architecture design session (#2). Until then this folder holds only this index.

This folder holds the system-level technical design: the overall **system architecture** (services + baseline + versioning), the **mesh** (Artemis broker, cluster discovery, cross-cluster interop, the `lattice-contract` envelopes), and the **deployment architecture** (Docker images, the Kubernetes cluster, Helm, environments). One doc per concern; this `_index.md` will become the overview.
