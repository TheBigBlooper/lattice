/**
 * What the mesh feature offers the rest of the console.
 *
 * The peer table, and nothing else. The activity log used to live here and moved out when it began
 * reporting this baseline's own services too - a panel covering both halves of the screen is not the
 * mesh feature's to own.
 */
export { DiscoveredBaselines } from "./DiscoveredBaselines.tsx";
