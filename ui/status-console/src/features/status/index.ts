/**
 * What the status feature offers the rest of the console.
 *
 * One screen. `ClusterVerdict` and `InfrastructureCard` are the panels it composes and stay
 * internal, so a future feature reaches for the screen rather than reaching past it.
 */
export { StatusView } from "./StatusView.tsx";
