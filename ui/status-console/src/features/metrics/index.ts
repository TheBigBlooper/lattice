/**
 * What the metrics feature offers the rest of the console.
 *
 * One screen. The cards, the sparkline, the series table and the reading hook are how it is built
 * and stay internal, so a future feature reaches for the screen rather than reaching past it.
 */
export { MetricsView } from "./MetricsView.tsx";
