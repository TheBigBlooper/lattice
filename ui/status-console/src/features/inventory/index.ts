/**
 * What the inventory feature offers the rest of the console.
 *
 * The screen, and nothing else. The reserve panel and the set-stock confirmation are what it
 * composes and stay internal, so the shell mounts a destination rather than assembling one.
 */
export { InventoryView } from "./InventoryView.tsx";
