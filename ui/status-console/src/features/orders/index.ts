/**
 * What the orders feature offers the rest of the console.
 *
 * The screen, and nothing else. `NewOrderForm` is the panel it composes and stays internal, so the
 * shell mounts a destination rather than assembling one.
 */
export { OrdersView } from "./OrdersView.tsx";
