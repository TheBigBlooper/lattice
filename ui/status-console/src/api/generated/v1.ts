/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/api/v1/baseline": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * This cluster's baseline identity and health.
         * @description Returns this cluster's baseline identity (cluster id, region, baseline version, and the API major versions it serves) plus its health rollup and the per-service readiness behind it. Served by the mesh-gateway. The health values are the ones last computed on the announce heartbeat, so this stays cheap and never blocks on a slow service.
         */
        get: operations["getBaseline"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/metrics": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * This service's selected meters.
         * @description Returns a selected set of this service's meters, for the console's Metrics view. Every service serves its own; the console reads each and renders them together.
         *     This exists because the Prometheus scrape endpoint is unreachable from a browser: it is on a separate management port, that port's Service is never published, and it carries no token. That endpoint is unchanged and remains what a collector scrapes; this is a narrower, guarded reader for the console. The set is deliberately selected rather than complete: it carries Lattice's own meters only, while the runtime and toolkit families the scrape endpoint also exposes stay there.
         */
        get: operations["getMetrics"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/peers": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The baselines this cluster has discovered on the mesh.
         * @description Returns every peer this cluster has heard announce itself, with the endpoints Shape A federation needs: consoleUrl is where an operator is redirected to act on that baseline, and apiBaseUrl is recorded but never read from a browser (locked #61). A peer that has gone silent past the liveness time-to-live is included as UNREACHABLE with its last-known snapshot rather than removed, so an operator sees a baseline went quiet instead of it vanishing. Served by the mesh-gateway from its in-memory registry; a cluster never lists itself.
         */
        get: operations["getPeers"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/orders": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * A page of this baseline's orders, newest first.
         * @description Returns one page of this baseline's orders sorted by createdAt descending, so the most recent work is the first thing an operator sees. It takes a page position and a page size and nothing else: there are deliberately no filters and no free-text query. A shared search contract would commit every baseline to identical query semantics, while each keeps its own possibly-divergent data model, and under Shape A nobody queries a peer's data anyway. The counts for the page live in meta.pagination.
         */
        get: operations["listOrders"];
        put?: never;
        /**
         * Create an order with its lines.
         * @description Creates an order from a strict request body (customerId + at least one line). The server mints the orderId (UUID) and createdAt (UTC), sets status RECEIVED, persists to Elasticsearch, and returns the created order in the success envelope. A client-supplied id is never trusted; the request body carries no id.
         */
        post: operations["createOrder"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/orders/{orderId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Retrieve an order by id.
         * @description Returns the persisted order for the given id in the success envelope, or a NOT_FOUND error envelope when no order has that id.
         */
        get: operations["getOrder"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/inventory": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * A page of this baseline's stock items, by sku.
         * @description Returns one page of this baseline's stock items sorted by sku. Sorted by sku rather than by recency because inventory is a catalogue an operator scans for a known item, where orders are a feed they read from the top. Takes a page position and a page size and nothing else, for the same reason as listOrders. The counts for the page live in meta.pagination.
         */
        get: operations["listInventory"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/inventory/{sku}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read an item's availability.
         * @description Returns the item's on-hand, reserved, and computed available (on-hand minus reserved, never stored) in the success envelope, or a NOT_FOUND error envelope when no item has that sku.
         */
        get: operations["getInventory"];
        /**
         * Set an item's absolute on-hand stock.
         * @description Sets the item's on-hand to the absolute value in the strict request body, creating the item (reserved 0) when absent. Rejects with CONFLICT when the new on-hand would fall below the currently reserved quantity (available would go negative). Written under optimistic concurrency. Returns the item (with computed available) in the success envelope.
         */
        put: operations["setStock"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/inventory/reservations": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Reserve stock against an order line.
         * @description Reserves a quantity of a sku against the order line (orderId, sku) from the strict request body. Oversell-safe: an unknown sku is NOT_FOUND, insufficient available stock is CONFLICT. Idempotent by (orderId, sku): a repeat with the same quantity returns the existing reservation without a second increment; a repeat with a different quantity is CONFLICT. The server mints the reservationId (UUID) and createdAt (UTC).
         */
        post: operations["createReservation"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Liveness probe (operational, unversioned, non-enveloped).
         * @description Liveness: reports that the process is up, with no dependency checks. Operational shape {status, checks}, not the business envelope. Read directly by Kubernetes probes, the deploy smoke, and the status console.
         */
        get: operations["getHealth"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/readiness": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Readiness probe (operational, unversioned, non-enveloped).
         * @description Readiness: 200 when every registered dependency check passes, 503 when any check is DOWN, so an orchestrator pulls a not-ready pod out of rotation. Operational shape {status, checks}, not the business envelope.
         */
        get: operations["getReadiness"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description A short label, name, or id. Reused wherever a request body caps a short string. */
        ShortString: string;
        /** @description A description or summary. Reused wherever a request body caps a medium string. */
        MediumString: string;
        /** @description A free-text body. Reused wherever a request body caps a long free-text field. */
        LongText: string;
        /** @description The envelope metadata block, present on every response (success and error). */
        Meta: {
            requestId: components["schemas"]["ShortString"];
            /**
             * @description The API major version serving the response, e.g. v1.
             * @example v1
             */
            apiVersion: string;
            pagination?: components["schemas"]["Pagination"];
        };
        /** @description Offset-pagination counts, present only in meta on list responses. */
        Pagination: {
            /** @description The zero-based page index. */
            page: number;
            /** @description The page size (1..100). */
            size: number;
            /** @description The total number of items across all pages. */
            total: number;
            /** @description The total number of pages. */
            totalPages: number;
        };
        /**
         * @description The fixed, machine-branchable error taxonomy. One code per HTTP status family.
         * @enum {string}
         */
        ErrorCode: "VALIDATION_ERROR" | "UNAUTHORIZED" | "FORBIDDEN" | "NOT_FOUND" | "CONFLICT" | "RATE_LIMITED" | "INTERNAL" | "UNAVAILABLE";
        /** @description A single field-level problem, populated for validation failures. */
        ErrorDetail: {
            field: components["schemas"]["ShortString"];
            issue: components["schemas"]["MediumString"];
        };
        /** @description The machine-branchable error body; clients branch on code, never on message. */
        ErrorObject: {
            code: components["schemas"]["ErrorCode"];
            /** @description A human-readable summary for logs or display. Never the branch key. */
            message: string;
            /** @description Field-level problems, populated for validation failures; omitted otherwise. */
            details?: components["schemas"]["ErrorDetail"][];
        };
        /** @description The failure envelope, exactly one of data|error carried alongside meta. */
        ErrorResponse: {
            error: components["schemas"]["ErrorObject"];
            meta: components["schemas"]["Meta"];
        };
        /** @description This cluster's baseline identity. */
        Baseline: {
            clusterId: components["schemas"]["ShortString"];
            region: components["schemas"]["ShortString"];
            /**
             * @description The versioned baseline (services + endpoints) this cluster runs, e.g. 0.1.0.
             * @example 0.1.0
             */
            baselineVersion: string;
            /** @description The API major versions this cluster serves, e.g. [v1]. */
            apiVersions: components["schemas"]["ShortString"][];
            health?: components["schemas"]["ClusterHealth"];
            /** @description Per-service readiness behind this cluster's health rollup. Served only here, to the owning baseline's own console: peers receive the rolled-up label alone, so the mesh envelope does not grow with every service added. */
            services?: components["schemas"]["ServiceHealth"][];
            /** @description The infrastructure this baseline's services depend on, as a sibling of the service breakdown. Served only here and never announced: locked #43 stands unamended, so the verdict a peer reads stays a rollup of this baseline's own services and a datastore that has merely lost a replica cannot make a peer believe this baseline is unable to serve. Optional and absent when nothing is configured, which is a supported deployment rather than a fault - so adding this does not break a client generated before it existed. */
            infrastructure?: components["schemas"]["ComponentHealth"][];
            meshLink?: components["schemas"]["MeshLinkState"];
        };
        /**
         * @description Whether this cluster can currently reach the mesh at all. A cluster whose broker link is down hears nothing, so its peer registry ages every peer to UNREACHABLE at once - which reads as "the whole mesh died" when the truth is "we are the ones cut off". Those are different incidents with different responses, so the gateway reports its own link state rather than leaving an operator to infer it. Served here only and never announced: a report about a broken link cannot travel over that link (locked #46, same reasoning as #43). Optional, so adding it does not break a generated client built before it existed.
         * @example up
         * @enum {string}
         */
        MeshLinkState: "up" | "down";
        /**
         * @description A cluster's rolled-up health, computed from its services' readiness. ready = every service is up; degraded = some are up; down = none could be reached, though the cluster is still announcing itself.
         * @example ready
         * @enum {string}
         */
        ClusterHealth: "ready" | "degraded" | "down";
        /** @description One service's readiness, as seen by this cluster's mesh-gateway. */
        ServiceHealth: {
            name: components["schemas"]["ShortString"];
            /**
             * @description UP when the service's readiness probe returned 200; DOWN otherwise.
             * @example UP
             * @enum {string}
             */
            status: "UP" | "DOWN";
        };
        /** @description One infrastructure component's state, as seen by this cluster's mesh-gateway. A separate shape from ServiceHealth rather than a widening of it: a component carries a kind and a detail line that mean nothing on a service, and a service's two states cannot express DEGRADED. */
        ComponentHealth: {
            name: components["schemas"]["ShortString"];
            /**
             * @description Which piece of infrastructure this is, and so which probe produced the status. Carried explicitly rather than inferred from the name, so a deployment may label a component whatever it calls it while the gateway still knows what it is talking to.
             * @example elasticsearch
             * @enum {string}
             */
            kind: "elasticsearch" | "artemis" | "keycloak";
            /**
             * @description The coarse state the console colours and counts. Each component speaks its own vocabulary (Elasticsearch green/yellow/red, Artemis connected or not, Keycloak up or not), so a shared three-word state lets the console render a component it has never seen, while detail keeps the native reading. UP = fully healthy; DEGRADED = serving with something genuinely lost, such as missing replicas; DOWN = unable to serve, unreachable, or not connected.
             * @example UP
             * @enum {string}
             */
            status: "UP" | "DEGRADED" | "DOWN";
            detail?: components["schemas"]["MediumString"];
        };
        /** @description Another baseline discovered on the mesh, as this cluster last heard it. An unreachable peer is retained with its last-known detail rather than removed, so an operator sees that a baseline was present and has gone silent. */
        Peer: {
            clusterId: components["schemas"]["ShortString"];
            region: components["schemas"]["ShortString"];
            /**
             * @description The baseline the peer last reported running.
             * @example 0.1.0
             */
            baselineVersion: string;
            health: components["schemas"]["ClusterHealth"];
            /**
             * @description The peer's own console root - where a "go to this baseline" redirect lands.
             * @example https://east.console:3000
             */
            consoleUrl: string;
            /**
             * @description The peer's REST API base. Advertised and recorded, but never read from a browser: every operation on it is bearer-protected against that peer's own realm and realm membership is deliberately unsynchronized, so a fan-out would be refused by every peer. The unified view renders each peer from this baseline's own registry instead (locked #61).
             * @example https://east.svc:8080/api/v1
             */
            apiBaseUrl: string;
            /**
             * Format: date-time
             * @description When this cluster last heard the peer announce, by this cluster's own clock - never the peer's, so a peer's clock drift cannot mask or fake its liveness.
             */
            lastSeen: string;
            /**
             * @description REACHABLE while the peer has announced within the liveness time-to-live; UNREACHABLE once it has gone silent for longer.
             * @example REACHABLE
             * @enum {string}
             */
            reachability: "REACHABLE" | "UNREACHABLE";
            federation?: components["schemas"]["FederationState"];
        };
        /**
         * @description This broker's federation link to one peer broker - a different thing from the mesh link beside it, which is a baseline's connection to its OWN broker. One being healthy says nothing about the other: a revoked or expired certificate leaves the mesh link perfectly up while nothing crosses, which is what made a broken mesh read as a quiet one. It also moves first, a full liveness time-to-live before the peer it affects goes UNREACHABLE. up = the link is carrying announcements. down = it is not, and nothing local explains why. refused = it is not, AND this baseline's own certificate has expired, so the fault is here and the fix is a re-issue. That conclusion is drawn by the gateway, which holds the certificate check, rather than re-derived by a client. Revocation is deliberately not distinguished: it lives in the authority's revocation list, which a baseline does not hold, so a revoked but unexpired certificate reads down. Optional, and absent rather than defaulted when it could not be read - a baseline that cannot measure the link must say nothing rather than claim it is healthy. Adding it does not break a client generated before it existed (locked #80).
         * @example up
         * @enum {string}
         */
        FederationState: "up" | "down" | "refused";
        /** @description The success envelope for the discovered peer list. */
        PeerListResponse: {
            data: components["schemas"]["Peer"][];
            meta: components["schemas"]["Meta"];
        };
        /**
         * @description What a sample's value means. The distinction is load-bearing rather than descriptive: a counter's instantaneous value is close to meaningless and is read as a rate, while a gauge's value is the answer on its own.
         * @example COUNTER
         * @enum {string}
         */
        MetricKind: "COUNTER" | "GAUGE" | "TIMER";
        /** @description One measured value, with the labels identifying which series it is. A meter with several label sets becomes several samples rather than one sample carrying a list, because the console's series table is a flat list by nature. */
        MetricSample: {
            name: components["schemas"]["ShortString"];
            kind: components["schemas"]["MetricKind"];
            /**
             * @description The label set identifying this series; absent or empty when it carries none.
             * @example {
             *       "source_cluster": "hub-east"
             *     }
             */
            labels?: {
                [key: string]: string;
            };
            /**
             * @description The value at the moment the snapshot was taken.
             * @example 46
             */
            value: number;
        };
        /** @description One service's selected meters at the moment it was asked. */
        MetricsSnapshot: {
            service: components["schemas"]["ShortString"];
            /** @description The selected samples, empty when the registry holds none yet - which is a real state for a service that has just started, not a failure. */
            samples: components["schemas"]["MetricSample"][];
        };
        /** @description The success envelope for a service's selected meters. */
        MetricsResponse: {
            data: components["schemas"]["MetricsSnapshot"];
            meta: components["schemas"]["Meta"];
        };
        /** @description The success envelope for the baseline identity. */
        BaselineResponse: {
            data: components["schemas"]["Baseline"];
            meta: components["schemas"]["Meta"];
        };
        /**
         * @description The declared order lifecycle status. Create sets RECEIVED; the later stages exist so the contract and mapping are stable when transition endpoints land (no transitions yet).
         * @enum {string}
         */
        OrderStatus: "RECEIVED" | "ALLOCATED" | "PACKED" | "SHIPPED" | "DELIVERED";
        /** @description One requested line of an order to create (strict; no client-supplied line id). */
        CreateOrderLine: {
            sku: components["schemas"]["ShortString"];
            /** @description The requested quantity for this line (at least 1). */
            quantity: number;
        };
        /** @description The strict create-order request body; the server mints id, status, and timestamp. */
        CreateOrderRequest: {
            customerId: components["schemas"]["ShortString"];
            /** @description The order lines (at least one, at most 100). */
            lines: components["schemas"]["CreateOrderLine"][];
        };
        /** @description One persisted line of an order (sku + quantity), lenient for forward-compatibility. */
        OrderLine: {
            sku: components["schemas"]["ShortString"];
            /** @description The quantity for this line. */
            quantity: number;
        };
        /** @description A persisted order, lenient for forward-compatibility (no additionalProperties bar). */
        Order: {
            orderId: components["schemas"]["ShortString"];
            customerId: components["schemas"]["ShortString"];
            status: components["schemas"]["OrderStatus"];
            /** @description The order's lines, as submitted. */
            lines: components["schemas"]["OrderLine"][];
            /**
             * Format: date-time
             * @description The server-set creation timestamp, always UTC (ISO-8601).
             */
            createdAt: string;
        };
        /** @description The success envelope for a page of orders. The page counts ride in meta.pagination, which the envelope already declares - a second paging block here would leave a client two answers to "how many are there". */
        OrderListResponse: {
            /** @description One page of orders, newest first. */
            data: components["schemas"]["Order"][];
            meta: components["schemas"]["Meta"];
        };
        /** @description The success envelope for a single order. */
        OrderResponse: {
            data: components["schemas"]["Order"];
            meta: components["schemas"]["Meta"];
        };
        /** @description The strict set-stock request body; sets the item's absolute on-hand quantity. */
        SetStockRequest: {
            /** @description The absolute on-hand quantity to set (zero or more). */
            onHand: number;
        };
        /** @description An item's stock, lenient for forward-compatibility. available is computed (onHand minus reserved) at read and never stored. */
        InventoryItem: {
            sku: components["schemas"]["ShortString"];
            /** @description The absolute on-hand quantity. */
            onHand: number;
            /** @description The quantity currently reserved against orders. */
            reserved: number;
            /** @description The computed available quantity (onHand minus reserved). */
            available: number;
            /** @description Where this baseline holds the item. Optional, and absent entirely on a baseline whose own Elasticsearch model does not record one - each cluster owns its own, possibly divergent model, so this is a field some baselines have and others do not. */
            binLocation?: components["schemas"]["ShortString"];
        };
        /** @description The success envelope for a page of stock items. The page counts ride in meta.pagination, which the envelope already declares. */
        InventoryListResponse: {
            /** @description One page of stock items, ordered by sku. */
            data: components["schemas"]["InventoryItem"][];
            meta: components["schemas"]["Meta"];
        };
        /** @description The success envelope for a single inventory item. */
        InventoryItemResponse: {
            data: components["schemas"]["InventoryItem"];
            meta: components["schemas"]["Meta"];
        };
        /** @description The strict create-reservation request body; the server mints the id and timestamp. */
        CreateReservationRequest: {
            orderId: components["schemas"]["ShortString"];
            sku: components["schemas"]["ShortString"];
            /** @description The quantity to reserve (at least 1). */
            quantity: number;
        };
        /** @description A persisted reservation of stock against an order line, lenient for forward-compatibility. */
        Reservation: {
            reservationId: components["schemas"]["ShortString"];
            orderId: components["schemas"]["ShortString"];
            sku: components["schemas"]["ShortString"];
            /** @description The reserved quantity. */
            quantity: number;
            /**
             * Format: date-time
             * @description The server-set creation timestamp, always UTC (ISO-8601).
             */
            createdAt: string;
        };
        /** @description The success envelope for a single reservation. */
        ReservationResponse: {
            data: components["schemas"]["Reservation"];
            meta: components["schemas"]["Meta"];
        };
        /** @description One registered health check with its name and current status. */
        HealthCheck: {
            name: components["schemas"]["ShortString"];
            status: components["schemas"]["HealthState"];
        };
        /**
         * @description The operational status of a process or dependency check.
         * @enum {string}
         */
        HealthState: "UP" | "DOWN";
        /** @description The operational probe shape (not the business envelope): an aggregate status plus one entry per registered check. */
        HealthStatus: {
            status: components["schemas"]["HealthState"];
            /** @description One entry per registered check. */
            checks: components["schemas"]["HealthCheck"][];
        };
    };
    responses: {
        /** @description Request failed contract validation (VALIDATION_ERROR). */
        BadRequest: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description No bearer token was presented, or it failed validation against this baseline's realm (UNAUTHORIZED). An expired token and a token issued by a peer baseline both land here. */
        Unauthorized: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description The token is valid but its role does not permit the operation (FORBIDDEN) - typically a `viewer` token attempting a write. */
        Forbidden: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description The addressed resource does not exist (NOT_FOUND). */
        NotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description The request conflicts with current state (CONFLICT). */
        Conflict: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Too many requests (RATE_LIMITED). */
        RateLimited: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Unhandled server error (INTERNAL); never leaks internals in message. */
        InternalError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description A dependency is down or the service is not ready (UNAVAILABLE). */
        Unavailable: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
    };
    parameters: {
        /** @description The zero-based page to return. Declared once and shared by every list operation, so paging cannot drift into two conventions across the contract. */
        PageIndex: number;
        /** @description How many items to return, 1..100. The ceiling is the contract refusing to let a client ask for an unbounded page: a baseline with a large index would otherwise be one request away from serving its whole collection in a single response. */
        PageSize: number;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getBaseline: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The baseline identity, wrapped in the success envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BaselineResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["Unavailable"];
        };
    };
    getMetrics: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The service's selected meters, wrapped in the success envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MetricsResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["Unavailable"];
        };
    };
    getPeers: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The discovered peers, wrapped in the success envelope. Empty before any peer announces. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PeerListResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["Unavailable"];
        };
    };
    listOrders: {
        parameters: {
            query?: {
                /** @description The zero-based page to return. Declared once and shared by every list operation, so paging cannot drift into two conventions across the contract. */
                page?: components["parameters"]["PageIndex"];
                /** @description How many items to return, 1..100. The ceiling is the contract refusing to let a client ask for an unbounded page: a baseline with a large index would otherwise be one request away from serving its whole collection in a single response. */
                size?: components["parameters"]["PageSize"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of orders, wrapped in the success envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrderListResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["Unavailable"];
        };
    };
    createOrder: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateOrderRequest"];
            };
        };
        responses: {
            /** @description The order was created, wrapped in the success envelope. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrderResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["Unavailable"];
        };
    };
    getOrder: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The server-minted order id (UUID). */
                orderId: components["schemas"]["ShortString"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The order, wrapped in the success envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrderResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["Unavailable"];
        };
    };
    listInventory: {
        parameters: {
            query?: {
                /** @description The zero-based page to return. Declared once and shared by every list operation, so paging cannot drift into two conventions across the contract. */
                page?: components["parameters"]["PageIndex"];
                /** @description How many items to return, 1..100. The ceiling is the contract refusing to let a client ask for an unbounded page: a baseline with a large index would otherwise be one request away from serving its whole collection in a single response. */
                size?: components["parameters"]["PageSize"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of stock items, wrapped in the success envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InventoryListResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["Unavailable"];
        };
    };
    getInventory: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The stock-keeping unit to read. */
                sku: components["schemas"]["ShortString"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The item, wrapped in the success envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InventoryItemResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["Unavailable"];
        };
    };
    setStock: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The stock-keeping unit the stock is set for. */
                sku: components["schemas"]["ShortString"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SetStockRequest"];
            };
        };
        responses: {
            /** @description The item's stock was set, wrapped in the success envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InventoryItemResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["Unavailable"];
        };
    };
    createReservation: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateReservationRequest"];
            };
        };
        responses: {
            /** @description The reservation was created, wrapped in the success envelope. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReservationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["Unavailable"];
        };
    };
    getHealth: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The process is up. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthStatus"];
                };
            };
            /** @description The process is not live. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthStatus"];
                };
            };
        };
    };
    getReadiness: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Every dependency check passes. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthStatus"];
                };
            };
            /** @description At least one dependency check is DOWN. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthStatus"];
                };
            };
        };
    };
}
