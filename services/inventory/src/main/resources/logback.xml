<?xml version="1.0" encoding="UTF-8"?>
<!--
  Inventory service logging (production binding). One console appender; root at INFO, the io.lattice
  application loggers at INFO. Vert.x's own logs arrive here too (MainVerticle routes them through
  the SLF4J delegate), so this is the single pipeline.
-->
<configuration>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <logger name="io.lattice" level="INFO" />

  <root level="INFO">
    <appender-ref ref="CONSOLE" />
  </root>
</configuration>
